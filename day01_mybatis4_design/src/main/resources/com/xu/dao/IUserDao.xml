<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="com.xu.dao.IUserDao"><!-- 写的是resources下的全限定类名-->
    <!-- 配置查询所有-->
    <select id="findAll" resultType="com.xu.domain.User"><!-- 一定是用户持久层接口中定义的方法-->
        select * from user
    </select>

</mapper>